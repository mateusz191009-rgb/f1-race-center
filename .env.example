# OpenF1 authentication (optional).
# If you have an OpenF1 subscription, copy this file to `.env` and fill these in.
# Credentials stay SERVER-SIDE in the Vite proxy (vite.config.ts) and are never
# exposed to the browser. Without them the app works anonymously.
OPENF1_USERNAME=
OPENF1_PASSWORD=
